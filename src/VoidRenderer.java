/**
 * A void renderer implementing the Renderer interface
 */
public class VoidRenderer implements Renderer {
    /**
     * Renders the board accordingly
     * @param board Board to render
     */
    @Override
    public void renderBoard(Board board) {
    }
}
